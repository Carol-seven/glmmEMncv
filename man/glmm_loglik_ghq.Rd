% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmm_loglik_ghq.R
\name{glmm_loglik_ghq}
\alias{glmm_loglik_ghq}
\title{GLMM Log-Likelihood via Gauss--Hermite Quadrature}
\usage{
glmm_loglik_ghq(y, X, W, beta, ni, Q, link = c("logit", "probit"))
}
\arguments{
\item{y}{A binary response vector of length \eqn{n}.}

\item{X}{An \eqn{n \times p} fixed-effects design matrix.}

\item{W}{An \eqn{n \times q} random-effects design matrix.}

\item{beta}{A vector of fixed-effects coefficients of length \eqn{p}.}

\item{ni}{An integer vector specifying the number of observations
within each cluster (e.g., subject-specific repeated measurements).
The sum of \code{ni} must equal \code{length(y)}.}

\item{Q}{An integer specifying the number of Gauss–Hermite quadrature points.
Larger values improve approximation accuracy but increase computation time.}

\item{link}{A character string specifying the link function.
Must be one of \code{"logit"} or \code{"probit"}.}
}
\value{
A scalar value representing the marginal log-likelihood of the GLMM.
}
\description{
Computes the marginal log-likelihood for a generalized linear mixed model
(GLMM) with binary outcomes using Gauss--Hermite quadrature (GHQ).
}
\details{
The model assumes
\deqn{ Y_{ij} \mid b_j \sim \mathrm{Bernoulli}(\pi_{ij}),}
where
\eqn{g(\pi_{ij}) = X_{ij}^\top \beta + W_{ij}^\top b_j}, and \eqn{g(\cdot)}
is either the logit or probit link function.

The likelihood contribution for each cluster is obtained by integrating over
the random effect distribution using Gauss-Hermite quadrature. The total
log-likelihood is the sum of cluster-specific marginal log-likelihoods.
}
